/**
 * CSS class configuration for styling various elements in a UI component.
 *
 * @typedef {Object} Classes
 * @property {string} titleClass - CSS classes for the title element, including font size, weight, and margin.
 * @property {string} summaryClass - CSS classes for the summary element, providing cursor interaction and font styling.
 * @property {string} detailsClass - CSS classes for the details element with margin styling.
 */

/**
 * Configuration object containing CSS classes for different elements in the component.
 *
 * @type {Classes}
 */
export const classes = {
  titleClass: 'font-bold text-[1.25rem] mt-[2.5rem]',
  summaryClass: 'cursor-pointer font-bold text-[1.25rem]',
  detailsClass: 'mt-[2.5rem]',
}
